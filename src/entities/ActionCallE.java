package entities;
/**
 * 
 * @author dominic
 * Die verschiedenen Ansagen
 * HOCHZEIT wird angesagt, damit man nicht still Hochzeit als Solo spielt
 */
public enum ActionCallE {
	KEINE90,
	KEINE60,
	KEINE30,
	RE,
	CONTRA,
	HOCHZEIT
}
